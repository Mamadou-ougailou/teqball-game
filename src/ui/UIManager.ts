import { IEntity } from '@core/interfaces';

/**
 * UIManager - Creates AdvancedDynamicTexture and owns all UI layers
 * TODO Phase 3: Show/hide UI by scene state
 */
export class UIManager implements IEntity {
  show(_uiPage: string): void {
    // TODO Phase 3
  }

  hide(_uiPage: string): void {
    // TODO Phase 3
  }

  update(_deltaTime: number): void {
    // TODO Phase 3
  }

  dispose(): void {
    // TODO Phase 3
  }
}
